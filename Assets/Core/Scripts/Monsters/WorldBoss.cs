using UnityEngine;
using Mirror;
namespace Game
{
    public class WorldBoss : Monster
    {
        /*protected override void Awake() {
            base.Awake();
            respawn = false;
        }
        [Server]
        protected override void OnDeath() {
            // take care of entity stuff
            base.OnDeath();
            print("World Boss Killed");
            ScheduledEventsHandler.singleton.EndWorldBoss();
        }*/
    }
}